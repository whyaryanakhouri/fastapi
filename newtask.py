# register api with validation password (confirm password)
# user name password return a session token (authentication token)
# store it in a table named authentication
# session has to pass to a third api (get and post)
# before giving post data the front end has to pass a token 
# we check it in database, if it exists, return 
# if not, then give error 403

# authentication table:
# user id ----- authentication token


# user table - username pswd email

# input username pswd email

